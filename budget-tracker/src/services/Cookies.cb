struct AppCookies
  session: string;

struct AppContext
  user_id: string;

namespace services_Cookies {
  let User (_s: AppCookies) =
    "session" in _s
      ? _s.session -> Session_UserId -> Users_User
      : null;
}