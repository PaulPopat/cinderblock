extends extensions/layout.pug
include components/form.pug
include components/notification.pug

block content
  if check_inbox
    +alert()
      | You will receive an email with a link to create your account
  if user_id
    p Your user ID #{user_id}.