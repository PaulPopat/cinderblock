extends extensions/layout.pug
include components/form.pug

block content
  ul
    each category in categories
      li= category.title
  
  form(action="/app/categories" method="post")
    +text("title", "text")
      | Category Title
    +button("submit")
      | Create